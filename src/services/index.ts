import * as githubService from "@src/services/github";
import * as localesService from "@src/services/locales";

const loggerService = { log: console.log };

export default {
  githubService,
  localesService,
  loggerService,
};
